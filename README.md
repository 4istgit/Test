# WABuffer (Western WA SMZ / No-Harvest Buffer Script)

## Run in VS Code terminal

1. Open `WABuffer.py`.
2. Edit the settings in the **User-provided parameters** section near the top (`flowlines_shp`, `out_gdb`, `type_field`, and `bankfull_field`).
3. Run:

```bash
python WABuffer.py
```
